/** What the trader confirms, per payment, before the agent pays through the
 *  payment gateway (modules/portals). Money never moves without it. */
export const PAYMENT_AUTHORISATION = "Payment authorisation";
